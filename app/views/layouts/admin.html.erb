<!DOCTYPE html>
<html>
  <head>
    <title>Renace - Admin</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top mb-4">
      <div class="container-fluid">
        <a class="navbar-brand" href="#">🧠 Renace Admin</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#adminNavbar">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="adminNavbar">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <%= link_to 'Usuarios', admin_users_path, class: 'nav-link' %>
            </li>
            <li class="nav-item">
              <%= link_to 'Cápsulas', admin_capsules_path, class: 'nav-link' %>
            </li>
            <li class="nav-item">
              <%= link_to 'Recuerdos', admin_memories_path, class: 'nav-link' %>
            </li>
          </ul>
          <ul class="navbar-nav">
            <li class="nav-item">
            <%= button_to 'Cerrar sesión', destroy_admin_session_path,
              method: :delete,
              class: 'btn btn-danger btn-sm',
              form: { class: 'd-inline' },
              data: { turbo: false } %>
            </li>
          </ul>
        </div>
      </div>
    </nav>

    <main class="container">
      <%= yield %>
    </main>
  </body>
</html>
